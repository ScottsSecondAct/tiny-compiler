fn factorial(n: int) -> int {
    var result: int = 1;
    var i: int = 1;
    while (i <= n) {
        result = result * i;
        i = i + 1;
    }
    return result;
}

print(factorial(5));
print(factorial(10));
